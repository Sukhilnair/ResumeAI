apiVersion: v1
kind: Service
metadata:
  name: {{ include "ResumeAI.fullname" . }}-frontend
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.frontEnd.containerPort }}
      targetPort: {{ .Values.frontEnd.containerPort }}
  selector:
    app: {{ include "ResumeAI.name" . }}-frontend